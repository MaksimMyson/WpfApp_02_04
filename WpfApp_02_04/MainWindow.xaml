<Window x:Class="WpfApp_02_04.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp_02_04"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Генерувати та зберегти числа" Click="GenerateAndSaveNumbers_Click" HorizontalAlignment="Center" Margin="0 10" 
                Style="{StaticResource MaterialDesignRaisedButton}" 
                materialDesign:ButtonAssist.CornerRadius="3"/>

        <TextBox x:Name="StatisticsTextBox" Grid.Row="2" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" IsReadOnly="True" 
                 Style="{StaticResource MaterialDesignTextBox}"/>
    </Grid>
</Window>
